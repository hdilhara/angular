import { AmericonStanderedDirective } from './americon-standered.directive';

describe('AmericonStanderedDirective', () => {
  it('should create an instance', () => {
    const directive = new AmericonStanderedDirective();
    expect(directive).toBeTruthy();
  });
});
